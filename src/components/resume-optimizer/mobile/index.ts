/**
 * Mobile Components Export Index
 *
 * Exports all mobile-optimized components for the Resume Optimizer
 * including responsive layouts, touch interactions, and mobile-first design
 */

export type {
  AccessibilityConfig,
  GestureConfig,
  ResponsiveLayoutProps,
} from '../types';
export { default as ResponsiveLayout } from './ResponsiveLayout';
